# Question 16: Write a Python program to print the ASCII value of a given character
# Input:
# Character = A

# Output:
# ASCII value = 65

# Explanation:
# Every character has a unique ASCII value. When a character is typecast to an integer, its ASCII value is obtained. The ASCII value of 'A' is 65

# Asked In Companies:

Char = input("Enter a character to get its ACII value: ")

print("The ASCII value of the enter Character is: ", ord(Char))
